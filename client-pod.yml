apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: web
spec:
  containers:
    - name: client
      image: registry.local:5000/client
      stdin: true 
      tty: true
      ports:
        - containerPort: 3000
